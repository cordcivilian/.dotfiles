[user]
	name = cordcivilian
	email = 69796618+cordcivilian@users.noreply.github.com
[credential]
	helper = store
[init]
	defaultBranch = main
[alias]
	graph = log --all --decorate --oneline --graph
	change-commits = change-commits = "!f() { VAR1=$1; VAR='$'$1; OLD=$2; NEW=$3; echo \"Are you sure for replace $VAR  $OLD => $NEW ?(Y/N)\";read OK;if [ \"$OK\" = 'Y' ] ; then shift 3; git filter-branch  --env-filter \"if [ \\\"${VAR}\\\" = '$OLD' ]; then export $VAR1='$NEW';echo 'to $NEW'; fi\" $@; fi;};  f "
